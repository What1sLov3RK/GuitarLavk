1)build docker image via:
docker build -t guitarlavk:latest .

Run with:
docker run --env-file .env -e PORT=8000 -p 8000:8000 guitarlavk:latest \
    sh -c "python manage.py collectstatic --noinput && gunicorn GuitarLavk.wsgi:application --bind 0.0.0.0:8000"





